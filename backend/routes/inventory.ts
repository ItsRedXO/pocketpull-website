import { Hono } from 'hono'; import { requireAuth,getBlinkServer,uid } from '../lib/auth'; import { processWalletTransaction } from '../lib/wallet';
const app=new Hono();
app.post('/inventory/lock',async c=>{try{const id=await requireAuth(c),{inventoryId,isLocked}=await c.req.json(),b=getBlinkServer(c.env as any);if(!inventoryId||typeof isLocked!=='boolean')return c.json({error:'inventoryId and isLocked required'},400);const card=await b.db.inventory.get(inventoryId) as any;if(!card)return c.json({error:'Card not found'},404);if(card.userId!==id)return c.json({error:'Not your card'},403);await b.db.inventory.update(inventoryId,{isLocked:isLocked?1:0});return c.json({success:true,inventoryId,isLocked})}catch(e:any){return c.json({error:e.message||'Authentication required'},401)}});
app.post('/inventory/favorite',async c=>{try{const id=await requireAuth(c),{inventoryId,isFavorite}=await c.req.json(),b=getBlinkServer(c.env as any);if(!inventoryId||typeof isFavorite!=='boolean')return c.json({error:'inventoryId and isFavorite required'},400);const card=await b.db.inventory.get(inventoryId) as any;if(!card)return c.json({error:'Card not found'},404);if(card.userId!==id)return c.json({error:'Not your card'},403);await b.db.inventory.update(inventoryId,{isFavorite:isFavorite?1:0});return c.json({success:true,inventoryId,isFavorite})}catch(e:any){return c.json({error:e.message||'Authentication required'},401)}});
app.post('/inventory/sell',async c=>{try{const id=await requireAuth(c),{inventoryId}=await c.req.json(),b=getBlinkServer(c.env as any),card=await b.db.inventory.get(inventoryId) as any;if(!card)return c.json({error:'Card not found'},404);if(card.userId!==id)return c.json({error:'Not your card'},403);if(Number(card.isLocked)>0)return c.json({error:'Card is locked and cannot be sold'},400);const value=Number(card.value||0);const w=await processWalletTransaction(b,{userId:id,type:'sell',amount:value,sourceId:inventoryId});if(!w.success)return c.json({error:w.error||'Failed to credit balance'},500);const removed=await b.db.query('DELETE FROM inventory WHERE id=$1 AND user_id=$2 AND is_locked=0 RETURNING id',[inventoryId,id]);if(!removed.rows.length)return c.json({error:'Card was already changed'},409);await b.db.transactions.create({id:`txn_${uid()}`,userId:id,type:'sell',amount:value,description:`Sold ${card.cardName} from inventory`});return c.json({success:true,newBalance:w.balanceAfter,soldCardId:inventoryId,cardValue:value})}catch(e:any){return c.json({error:e.message||'Internal server error'},500)}});
app.post('/inventory/sell-all',async c=>{try{const id=await requireAuth(c),b=getBlinkServer(c.env as any),cards=await b.db.inventory.list({where:{userId:id,isLocked:0}}) as any[];if(!cards.length)return c.json({error:'No unlocked cards to sell'},400);const ids=cards.map(x=>x.id),value=cards.reduce((s,x)=>s+Number(x.value||0),0),removed=await b.db.query('DELETE FROM inventory WHERE user_id=$1 AND is_locked=0 AND id=ANY($2::text[]) RETURNING id',[id,ids]);if(removed.rows.length!==ids.length)return c.json({error:'Inventory changed; please try again'},409);const w=await processWalletTransaction(b,{userId:id,type:'sell_all',amount:value,sourceId:ids.join(',').slice(0,100)});if(!w.success)return c.json({error:w.error||'Failed to credit balance'},500);await b.db.transactions.create({id:`txn_${uid()}`,userId:id,type:'sell',amount:value,description:`Sold all ${cards.length} unlocked card(s) from inventory`});return c.json({success:true,newBalance:w.balanceAfter,soldCardIds:ids,totalValue:value,count:cards.length})}catch(e:any){return c.json({error:e.message||'Internal server error'},500)}});export default app;
