import { blink } from '../lib/blink';
const BACKEND_BASE=import.meta.env.VITE_BACKEND_URL||'https://b2nnhe2n.backend.blink.new';
async function headers(){const token=await blink.auth.getValidToken().catch(()=>null);const pass=localStorage.getItem('pocketpull_admin_pass');return{'Content-Type':'application/json',...(token?{Authorization:`Bearer ${token}`}:{ }),...(pass?{'X-Admin-Password':pass}:{})};}
async function request<T>(path:string,init:RequestInit={}):Promise<T>{const res=await fetch(`${BACKEND_BASE}${path}`,{...init,headers:{...(await headers()),...(init.headers||{})}});const data=await res.json().catch(()=>({}));if(!res.ok)throw new Error(data?.error||`API error ${res.status}`);return data as T;}
export const adminPacksApi={login:async(identifier:string,password:string)=>{const res=await fetch(`${BACKEND_BASE}/admin/auth/login`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({identifier,password})});const data=await res.json().catch(()=>({}));if(!res.ok)throw new Error(data?.error||'Invalid username or password.');return data as {success:boolean;adminPassword:string};},list:()=>request<{packs:any[];cards:any[]}>('/admin/packs'),save:(pack:any,cards:any[])=>request<{success:boolean}>('/admin/packs',{method:'POST',body:JSON.stringify({pack,cards})}),update:(id:string,patch:any)=>request<{success:boolean}>(`/admin/packs/${encodeURIComponent(id)}`,{method:'PATCH',body:JSON.stringify(patch)}),remove:(id:string)=>request<{success:boolean}>(`/admin/packs/${encodeURIComponent(id)}`,{method:'DELETE'})};
