import { seededRandom, getDailySeed } from './simulation';
export interface LeaderboardEntry { rank:number; user:string; value:string; numericValue:number; sub:string; avatar:string; isReal?:boolean; }
function getAvatarEmoji(username=''){const emojis=['🔥','🌈','🌙','🧬','🐉','🔴','💜','🌌','🌊','👑','📦','🏆','🔒','💛','⚡','✨'];if(!username)return'👤';let hash=0;for(let i=0;i<username.length;i++)hash=username.charCodeAt(i)+((hash<<5)-hash);return emojis[Math.abs(hash)%emojis.length];}
const cache=new Map<string,{data:LeaderboardEntry[];expiresAt:number}>();const pending=new Map<string,Promise<LeaderboardEntry[]>>();
const BACKEND_BASE=import.meta.env.VITE_BACKEND_URL||'https://b2nnhe2n.backend.blink.new';
async function load(type:'pulls'|'packs'|'upgrades'):Promise<LeaderboardEntry[]>{const seed=getDailySeed();let real:any[]=[];try{const res=await fetch(`${BACKEND_BASE}/leaderboard/${type}`);if(res.ok){const data=await res.json();real=Array.isArray(data.entries)?data.entries:[];}}catch{}const realEntries=real.map(r=>({user:r.username||r.user||'Trainer',numericValue:Number(r.numericValue??r.value??0),avatar:r.avatar||getAvatarEmoji(r.username||r.user),isReal:true}));const rng=seededRandom(seed+type), existing=new Set(realEntries.map(e=>e.user.toLowerCase()));const simulated:any[]=[];for(let i=0;i<100;i++){const username=`Trainer_${String(Math.floor(rng()*9000)+1000)}`;if(existing.has(username.toLowerCase()))continue;const entryRng=seededRandom(seed+type+username);const val=type==='pulls'?100+(1-i/100)*2000+entryRng()*500:type==='packs'?50+(1-i/100)*4000+entryRng()*300:20+(1-i/100)*300+entryRng()*100;simulated.push({user:username,numericValue:val,avatar:getAvatarEmoji(username),isReal:false});}return[...realEntries,...simulated].sort((a,b)=>b.numericValue-a.numericValue).slice(0,100).map((e,i)=>({rank:i+1,user:e.user,numericValue:e.numericValue,value:type==='pulls'?`$${e.numericValue.toLocaleString(undefined,{minimumFractionDigits:2,maximumFractionDigits:2})}`:`${Math.floor(e.numericValue).toLocaleString()} ${type==='packs'?'packs':'attempts'}`,sub:type==='pulls'?(e.isReal?'Record pull':'Pulled Rare Card'):type==='packs'?'Total opened':(e.isReal?'Upgrades today':'Upgrader usage today'),avatar:e.avatar,isReal:e.isReal}));}
export async function getLeaderboardData(type:'pulls'|'packs'|'upgrades',userId?:string){const key=`${type}:${userId||'public'}`,hit=cache.get(key);if(hit&&hit.expiresAt>Date.now())return hit.data;const wait=pending.get(key);if(wait)return wait;const req=load(type).then(data=>{cache.set(key,{data,expiresAt:Date.now()+30_000});return data;}).finally(()=>pending.delete(key));pending.set(key,req);return req;}
