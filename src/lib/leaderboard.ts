import { generateUsername,seededRandom,getDailySeed } from './simulation';
export interface LeaderboardEntry{rank:number;user:string;value:string;numericValue:number;sub:string;avatar:string;isReal?:boolean;}
const cache=new Map<string,{data:LeaderboardEntry[];expiresAt:number}>();const pending=new Map<string,Promise<LeaderboardEntry[]>>();
const API=import.meta.env.VITE_API_BASE_URL||'/api';
const emoji=(u:string='')=>{const e=['🔥','🌈','🌙','🧬','🐉','🔴','💜','🌌','🌊','👑','📦','🏆','🔒','💛','⚡','✨'];let h=0;for(let i=0;i<u.length;i++)h=u.charCodeAt(i)+((h<<5)-h);return e[Math.abs(h)%e.length]||'👤';};
async function load(type:'pulls'|'packs'|'upgrades'):Promise<LeaderboardEntry[]>{const r=await fetch(`${API}/leaderboard?type=${type}`);const d=await r.json();if(!r.ok)throw new Error(d?.error||'Failed to load leaderboard');const real=(d.entries||[]).map((x:any)=>({user:x.username||'Trainer',numericValue:Number(type==='pulls'?x.biggest_pull:type==='packs'?x.packs_opened:x.upgrades_attempted)||0,avatar:emoji(x.username),isReal:true}));const cards=d.cards||[];const seed=getDailySeed();const rng=seededRandom(seed+type);const existing=new Set(real.map((x:any)=>x.user.toLowerCase()));const now=new Date();const progress=(now.getHours()*60+now.getMinutes())/1440;const simulated:any[]=[];for(let i=0;i<100&&simulated.length<100;i++){const user=generateUsername(seed+type+i);if(existing.has(user.toLowerCase()))continue;const er=seededRandom(seed+type+user),ar=seededRandom(seed+type+user+Math.floor(Date.now()/10000));let value=0,cardName='';if(type==='pulls'){if(cards.length){const card=cards[Math.min(cards.length-1,Math.floor((i/100)*cards.length))];value=Math.min(Number(cards[0]?.estimated_value||0),Number(card.estimated_value||0)*(0.8+progress*.2)+(ar()*10-5));cardName=card.card_name||'';}else value=100+(1-i/100)*2000+progress*500;}else if(type==='packs')value=50+(1-i/100)*4000+progress*(200+er()*300)+ar()*2;else value=20+(1-i/100)*300+progress*100+ar()*3;simulated.push({user,numericValue:Math.max(0,value),avatar:emoji(user),isReal:false,cardName});}
const all=[...real,...simulated].sort((a,b)=>b.numericValue-a.numericValue).slice(0,100);return all.map((e:any,i)=>({rank:i+1,user:e.user,numericValue:e.numericValue,value:type==='pulls'?`$${e.numericValue.toLocaleString(undefined,{minimumFractionDigits:2,maximumFractionDigits:2})}`:type==='packs'?`${Math.floor(e.numericValue).toLocaleString()} packs`:`${Math.floor(e.numericValue).toLocaleString()} attempts`,sub:type==='pulls'?(e.isReal?'Record pull':`Pulled ${e.cardName||'Rare Card'}`):type==='packs'?'Total opened':(e.isReal?'Upgrades today':'Upgrader usage today'),avatar:e.avatar,isReal:e.isReal}));}
export async function getLeaderboardData(type:'pulls'|'packs'|'upgrades',userId?:string){const key=`${type}:${userId||'public'}`,cached=cache.get(key);if(cached&&cached.expiresAt>Date.now())return cached.data;const p=pending.get(key);if(p)return p;const req=load(type).then(data=>{cache.set(key,{data,expiresAt:Date.now()+30000});return data;}).finally(()=>pending.delete(key));pending.set(key,req);return req;}
